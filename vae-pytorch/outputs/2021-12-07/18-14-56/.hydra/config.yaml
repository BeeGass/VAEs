models:
  encoder: vanilla
  decoder: vanilla
  hidden_dim: 50
  hidden_sub_dim: 20
train:
  test: false
  debug: false
  num_epochs: 100
  batch_size: 10
  weight_decay: 0.001
  learning_rate: 0.001
  num_workers: 22
  beta: 1
wandb:
  log_metrics: true
  watch_loss: true
